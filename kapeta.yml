kind: core/resource-type-internal
metadata:
  name: kapeta/resource-type-web-page
  title: Web Page
  description: Create web-based UI's for your plan.
  visibility: public
spec:
  ports:
    - name: http
      type: web

---
kind: core/resource-type-internal
metadata:
  name: kapeta/resource-type-web-fragment
  title: Web Fragment
  description: Consume web page fragments to enable a microfrontend architecture
  visibility: public
spec:
  ports:
    - name: http
      type: web

